plugins {
  id 'java'
}

group = 'com.sseob'
version = '0.0.1-SNAPSHOT'

java {
  toolchain {
    languageVersion = JavaLanguageVersion.of(21)
  }
}

repositories {
  mavenCentral()
}

dependencies {
  compileOnly'org.projectlombok:lombok'
}

tasks.named('test') {
  useJUnitPlatform()
}
